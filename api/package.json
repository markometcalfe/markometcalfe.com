{
  "name": "markometcalfe.com-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash dev.sh",
    "lint": "oxlint . && oxfmt --check .",
    "lint:fix": "oxlint . --fix && oxfmt ."
  },
  "dependencies": {
    "obscenity": "^0.4.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "wrangler": "^4.0.0"
  },
  "lint-staged": {
    "**/*.ts": [
      "oxlint --fix",
      "oxfmt",
      "sh -c 'tsc --noEmit'"
    ]
  }
}
